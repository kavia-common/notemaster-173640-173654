# notes_backend environment variables (example)
#
# IMPORTANT:
# - Do NOT commit a real .env with secrets/paths for runtime.
# - The preview/orchestrator will inject these values.

# Path or sqlite URL for the SQLite DB created by notes_database.
# Examples:
#   SQLITE_DB=/absolute/path/to/myapp.db
#   SQLITE_DB=myapp.db
#   SQLITE_DB=sqlite:////absolute/path/to/myapp.db
SQLITE_DB=/path/to/myapp.db

# Comma-separated list of allowed origins for CORS (frontend preview URL, localhost, etc.)
ALLOWED_ORIGINS=http://localhost:3000

# Optional regex if you need to allow dynamic preview subdomains/origins
# ALLOWED_ORIGIN_REGEX=https://.*\\.cloud\\.kavia\\.ai(:\\d+)?$
